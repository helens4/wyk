package com.example.notesapp

import android.app.Application
import androidx.lifecycle.AndroidViewModel

class NotesViewModel(app: Application): AndroidViewModel(app) {

    val text = "chelo wordl"
}